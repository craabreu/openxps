name: linter
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - openmm
  - numpy
  - cvpack
  - pyyaml

    # Testing
  - openmmtools
  - pytest

    # Linting
  - ruff
