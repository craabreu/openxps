name: test
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - pip
  - openmm
  - numpy
  - cvpack
  - pyyaml
  - typing-extensions

    # Testing
  - openmmtools
  - pytest
  - pytest-cov

    # Machine learning dependencies
  - pytorch
  - lightning
