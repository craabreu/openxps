name: test
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - pip
  - openmm
  - numpy
  - cvpack
  - pyyaml

    # Testing
  - openmmtools
  - pytest
  - pytest-cov
